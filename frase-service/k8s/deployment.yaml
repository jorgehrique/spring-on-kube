apiVersion: apps/v1
kind: Deployment
metadata:
  name: frase-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frase-service
  template:
    metadata:
      labels:
        app: frase-service
    spec:
      containers:
        - name: frase-service
          image: jorgehrique/sok-frase-service:1.0.0
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 350m
              memory: 120Mi
            limits:
              cpu: 700m
              memory: 300Mi
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5